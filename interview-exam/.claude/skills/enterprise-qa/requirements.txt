# enterprise-qa skill dependencies
# Core: Python 3.10+ standard library (sqlite3, json, re, pathlib, argparse)
# Optional: pyyaml for config.yaml parsing (falls back to regex parsing if absent)
pyyaml>=6.0
pytest>=7.0
